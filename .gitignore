# Build artifacts
/build/
/.gocache/
go/cpa-plugin-codex-turn-state

# Node / Playwright dev deps (installed ad hoc to run tests/ui, see tests/ui/README.md)
/node_modules/
/package.json
/package-lock.json

# Claude Code local session config (not for the repo)
/.claude/

# 账号凭据导入文件,禁止入库
/gpt/
